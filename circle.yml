#
# Build configuration for Circle CI
#

general:
    artifacts:
        - /home/ubuntu/buildtest/app/build/outputs/apk/

machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-26.0.2,android-26
        - chmod +x gradlew
        - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies

test:

  pre:
      - android list targets
      - android create avd --name Default --target android-26 --abi no ABIs
      - echo 'vm.heapSize=512' >> ~/.android/avd/emulatorwithgoogleapi26.ini
      - echo 'hw.ramSize=1024' >> ~/.android/avd/emulatorwithgoogleapi26.ini
      - cat ~/.android/avd/emulatorwithgoogleapi26.ini
      - emulator -avd emulatorwithgoogleapi26 -no-audio -no-window :
          background: true
          parallel: true
      - circle-android wait-for-boot
      - adb shell input keyevent 82
      - adb shell svc power stayon true
      - adb shell settings put global window_animation_scale 0
      - adb shell settings put global transition_animation_scale 0
      - adb shell settings put global animator_duration_scale 0

  override:
    - ./gradlew assembleDebug
    - ./gradlew connectedAndroidTest

  post:
      - cp -r app/build/outputs $CIRCLE_ARTIFACTS
      - cp -r app/build/outputs/androidTest-results/ $CIRCLE_TEST_REPORTS